# ImgaeDrag
A native macOS menu bar utility to streamline asset collection. Start a session to automatically download dragged images and video links (via yt-dlp) directly into dynamic, timestamped folders. Supports custom quality presets and MP3 audio extraction. Clean, simple, and built with SwiftUI.
